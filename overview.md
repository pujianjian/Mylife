# 实物资产页面排版优化完成

## 修改内容

- 重新排版 `pages/assets/assets` 页面，解决「打开后里面是乱的」问题。
- 主要参考「有数」类实物资产管理 App 的截图风格，对四个 Tab 进行视觉重构。

### 有数 Tab
- 顶部总览卡片改为左右两列：总资产 / 资产数量。
- 下方用彩色状态点显示「服役中 / 已退役 / 已卖出」数量。
- 增加「资产列表」小标题，筛选 Tab 更紧凑。
- 资产卡片重新设计：圆形图标背景、状态标签、名称、价格、使用天数与日均成本，信息层级更清晰。

### 心愿 Tab
- 顶部使用渐变粉色总值卡片，展示心愿总值、已存金额、心愿数量。
- 每个心愿使用独立卡片：图标 + 名称 + 目标 + 完成百分比 + 进度条 + 已存/还差金额。

### 趋势 / 设置 Tab
- 优化卡片间距与标题层级，保持原有图表组件不变。

### 底部导航与悬浮按钮
- 内联 TabBar 和悬浮加号按钮间距加大，避免重叠。
- 增加 `env(safe-area-inset-bottom)` 适配 iPhone 底部安全区。

### 文件变更
- `pages/assets/assets.wxml`
- `pages/assets/assets.wxss`
- `pages/assets/assets.js`（心愿列表增加 remaining 字段）

## 后续建议

- 如果截图中有特定元素（如某个形象图、特殊布局比例），请重新上传图片，我可以继续微调。
